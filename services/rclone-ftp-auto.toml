name = "rclone-ftp-auto-mount"
command = "/opt/rclone-mount.sh ftp :/ /music/ftp --ftp-no-check-certificate --ftp-tls=false --ftp-explicit-tls=true"

[restart]
strategy = "never"
backoff = "0s"
attempts = 0

[environment]
keep-env = true
